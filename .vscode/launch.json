{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "gdb",
            "request": "launch",
            "name": "Launch Program",
            "preLaunchTask": "make",
            "target": "${workspaceFolder}/build/netlink",
            "cwd": "${workspaceFolder}",
            "valuesFormatting": "parseText",
        }
    ]
}